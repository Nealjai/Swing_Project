# Backtest UI/UX Revamp

- [ ] Restructure the backtest tab HTML in `docs/index.html`.
- [ ] Implement the "Backtest Explorer" UI with a filterable table.
- [ ] Implement the "Backtest Report" UI with collapsible accordions.
- [ ] Update `docs/app.js` to populate the new explorer table.
- [ ] Update `docs/app.js` to handle filtering logic for the explorer.
- [ ] Update `docs/app.js` to handle loading a selected run into the report view.
- [ ] Update `docs/styles.css` to style the new components.
